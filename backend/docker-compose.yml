services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
  web:
    build: .
    volumes:
      - .:/usr/src/bioAI/backend
    ports:
      - 8000:8000
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"

